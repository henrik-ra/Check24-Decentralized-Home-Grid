org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official

# Windows/OneDrive can create placeholder/reparse-point files that confuse Gradle's snapshotting.
# Disabling FS watching is a pragmatic fix for "Cannot snapshot ... not a regular file".
org.gradle.vfs.watch=false

# Keep builds deterministic in CI/dev; can be turned back on if needed.
org.gradle.configuration-cache=false

# --- Azure Deployment Configuration ---
# Automatically configured by GitHub Copilot
API_BASE_URL=https://c24-home-core-2yw4ry.nicecliff-bf76ea91.westeurope.azurecontainerapps.io/
